#include <stdlib.h>
#include <stdio.h>
double Rand(char *arg, int range)
{	
	//int n = 0, i;
///	n = atoi(arg);
//	srand(n);
//	for(i = 0; i < size; i++){
  //  		A[i] =  (char)(rand() % range + 'a');
    }

